Make sure to blacklist snd-hda-intel and add snd_intel_dspcfg.dsp_driver=1 to arch.conf

```bash
blacklist snd-hda-intel
```

```bash
sudo nano /boot/loader/entries/arch.conf
```

options root=PARTUUID=xxxx rw quiet splash snd_intel_dspcfg.dsp_driver=1
